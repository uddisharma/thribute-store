import ReferEarn from "@/components/Refer-earn/page";
import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "Refer & Earn",
};
const page = () => {
  return <ReferEarn />;
};

export default page;
