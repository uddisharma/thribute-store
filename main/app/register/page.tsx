import Register1 from "@/components/Register/page";
import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "Register",
};
const page = () => {
  return <Register1 />;
};

export default page;
